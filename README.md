In Linear search , we iterate over all the elements present in data structure and check if it the current element is equal to the target element.

Binary  search algoirthm used in sorted array by reapeatedly dividing the search interval in half.Its time complexity is O(log n).
